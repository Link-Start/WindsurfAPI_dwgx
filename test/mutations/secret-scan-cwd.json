{
  "tests": [
    "test/secret-scan-cwd.test.js"
  ],
  "expectBaselinePass": 2,
  "mutations": [
    {
      "name": "Caller cwd grants an unrelated file the repository allow-list identity",
      "file": "scripts/secret-scan.mjs",
      "anchor": "return relative(rootReal, realpathSync(resolve(root, file))).split(sep).join('/');",
      "replacement": "return relative(rootReal, realpathSync(file)).split(sep).join('/');",
      "expectCaught": true
    }
  ]
}
